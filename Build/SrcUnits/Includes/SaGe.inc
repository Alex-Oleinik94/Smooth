{$IFNDEF ANDROID}
	{$IFDEF UNIX}
		{$IFNDEF DARWIN}
			{$DEFINE LINUX}
			{$ENDIF}
		{$ENDIF}
	{$ENDIF}

{$IFDEF ANDROID}
	{$DEFINE MOBILE}
	//{$ASMMODE DIRECT}
	{$ENDIF}

{$IF (not defined(MOBILE)) and (not defined(WIN64)) and (not (defined(DARWIN) and defined(CPU32)))}
	{$IF (not defined(RELEASE)) or defined(ENABLELIBPNG)}
		{$DEFINE WITHLIBPNG}
		{$ENDIF}
	{$ENDIF}

{$IFDEF RELEASE}
	{$DEFINE SUPPORTINLINE}
	{$ENDIF}

{$IFDEF FPC}
	{$PACKRECORDS C}
	{$MODE OBJFPC}
	{$IFNDEF ANDROID}
		{$ASMMODE INTEL}
	{$ELSE}
		//{$ASMMODE ?}
		{$ENDIF}
	{$H+}
	{$ENDIF}

//{$DEFINE LAZARUS}
//{$DEFINE GLUT}

{$WARNINGS OFF}
{$NOTES OFF}
{$HINTS OFF}

{$DEFINE SGDebuging}
//{$DEFINE SGMoreDebuging}

{$IF defined(CPU386) and defined(CPU32) and (not defined(DARWIN))}
	{$DEFINE WITHASMINC}
	{$ENDIF}

{$IFNDEF MSWINDOWS}
	{$OVERFLOWCHECKS OFF} 
	{$ENDIF}

